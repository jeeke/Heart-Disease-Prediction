import {ResourceWithOptions} from "admin-bro";
import {Video} from "../../models/video.entity";

const VideoResource: ResourceWithOptions = {
    resource: Video,
    options: {},
};

export default VideoResource;
